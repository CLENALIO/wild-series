<form method="post" action="{{ path('app_actor_delete', {'slug': actor.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.slug) }}">
    <button class="btn btn-danger my-2 mx-4">Supprimer</button>
</form>
