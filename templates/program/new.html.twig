{% extends 'base.html.twig' %}

{% block title %}Nouvelle serie{% endblock %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Add program</button>
    {{ form_end(form) }}

    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}