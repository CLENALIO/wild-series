{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ season.year }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ season.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('season_index') }}"class="btn btn-info my-2 mx-4">Retour à la liste</a>

    <a href="{{ path('season_edit', {'id': season.id}) }}"class="btn btn-warning my-1 mx-4">Modifier</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
