{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}
    <h1>Episode</h1>

    <table class="table table-white table-hover table-borderless">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ episode.title }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ episode.number }}</td>
            </tr>
            <tr>
                <th>Synopsis</th>
                <td>{{ episode.synopsis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_episode_index') }}" class="btn btn-info my-2 mx-4">Retour à la liste</a>

    <a href="{{ path('app_episode_edit', {'id': episode.id}) }}" class="btn btn-warning my-1 mx-4">Modifier</a>

    {{ include('episode/_delete_form.html.twig') }}
{% endblock %}
